//! Webcash flavor binary. Stub; main() lands in M1 alongside the migration
//! of the existing `crates/server` runtime into `webycash-server-core`.

fn main() {
    eprintln!("server-webcash: M0 stub. Migration from crates/server lands in M1.");
}
